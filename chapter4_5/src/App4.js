import EventPractice from "./EventPractice4";

// state의 초깃값을 설정하고, 
// 이벤트 핸들링 함수 내부에서 this.setState 메서드를 호출하여 state를 업데이트
// input의 value 값을 state에 있는 값으로 설정
// input에 입력이 되면 성공

function App() {
  return (
<EventPractice />
  );
}

export default App;
