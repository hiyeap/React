import EventPractice from "./EventPractice5";

// 버튼을 누르면 입력한 값이 state에 들어갔는지 확인
// 클릭 이벤트가 발생하면 현재 comment 값을 메시지 박스로 띄운 후
// comment 값을 공백으로 설정

function App() {
  return (
<EventPractice />
  );
}

export default App;
