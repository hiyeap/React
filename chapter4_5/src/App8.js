import EventPractice from "./EventPractice8";

/* input 여러 개 다루기
=> event 객체를 활용, e.target.name 사용
객체 안에서 key를 []로 감싸면 그 안에 넣은 
레퍼런스가 가리키는 실제 값이 key 값으로 사용된다
*/

function App() {
  return (
<EventPractice />
  );
}

export default App;
